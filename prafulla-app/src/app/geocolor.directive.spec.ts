import { GeocolorDirective } from './geocolor.directive';

describe('GeocolorDirective', () => {
  it('should create an instance', () => {
    const directive = new GeocolorDirective();
    expect(directive).toBeTruthy();
  });
});
